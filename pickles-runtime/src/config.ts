import type { PicklesRuntimeConfig, PicklesNativeRule } from "./types.ts";

export const defineConfig = (config: PicklesRuntimeConfig): PicklesRuntimeConfig => {
    return config;
};

export const defineRule = (rule: PicklesNativeRule): PicklesNativeRule => {
    return rule;
};
